{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc --noEmit",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs",
    "test": "bun test src",
    "test:postgres": "bun test tests/integration/billing/repository.postgres.test.ts"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@hono/zod-validator": "^0.8.0",
    "@trpc/server": "catalog:",
    "@wincode/ai": "workspace:*",
    "@wincode/auth": "workspace:*",
    "@wincode/billing": "workspace:*",
    "@wincode/db": "workspace:*",
    "@wincode/env": "workspace:*",
    "@polar-sh/sdk": "catalog:",
    "ai": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@wincode/config": "workspace:*",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
