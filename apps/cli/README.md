# react

To install dependencies:

```bash
bun install
```

To run:

```bash
bun dev
```

This project was created using `bun create cli`. [create-cli](https://git.new/create-cli) is the easiest way to get started with OpenTUI.
